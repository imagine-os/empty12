/**
 * Orchestrator persistence (PAP-93's `contract_checks(issue_id, checked_at,
 * ok, violations_json)` table and PAP-96's claim/session bookkeeping).
 *
 * Stub only — no schema or driver chosen yet.
 */

// TODO(PAP-93): `contract_checks` table + query helpers.
// TODO(PAP-96): claim/session state (which worker holds which issue).

export {};
